define(function () { 'use strict';

    var Report = function Report(type, data) {
      this.type = type;
      this.data = data;
    };

    Report.prototype.isError = function () {
      return this.type === 'failure' || this.type === 'error';
    };

    return Report;

});
