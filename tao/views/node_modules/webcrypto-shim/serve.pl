#!/usr/bin/env perl
use Mojolicious::Lite;

my $app = app;
my $static = $app->static;
push @{$static->paths}, $ENV{PWD};

get '/' => sub {
    my $self = shift;
    $self->redirect_to('/index.html');
};

$app->start;
